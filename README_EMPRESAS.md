# 7_Actividad_clase_Empresas (estructura clonada del proyecto de ejemplo)

Mantiene TODAS las carpetas del ejemplo (android, ios, web, linux, macos, windows, test, build, etc.).
Solo se ha adaptado `lib/` y `pubspec.yaml` al dominio Empresas.

## Ejecutar
flutter pub get
flutter run -d chrome
